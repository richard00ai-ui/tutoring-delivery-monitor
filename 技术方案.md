# 督学交付质量监控 - 技术方案

> 版本:v1 / 2026-07-26

> **实施提示词见 [workbuddy指令.md](workbuddy指令.md)** —— 两段可直接粘给 workbuddy 的任务描述:定时调度 + RPA 抓取。

## 一、项目目标

每天扫一遍 400-500 个督学外部群的聊天记录,按预设规则判定当天服务是否完成,产出:
- 每群「完成 / 未完成」+ 缺失项
- 主管每日看板 + 未完成告警推送

## 二、关键前提

1. 群性质:**外部群**(含家长/学生),不能用群机器人
2. 规模:400-500 群
3. 判定标准:**已有明确规则**(3-5 条硬规则,如"发计划 + 打卡 + 回应")
4. 形态:**内部定时脚本 + 通知**,不做 Web 看板
5. 项目负责人:**本人在所有群里**,员工端 AI(大圆/服务总结)可用
6. **部署拓扑**:
   - 开发/搭建在本机(Mac)完成
   - 程序打包后部署到**客户的电脑**长期运行(客户电脑已登录企微、在所有督学群里)
   - 通过 **workbuddy** 托管保活和远程管理

---

## 三、方案选型

三条路径,按"起步速度 vs 长期资产"取舍。

### 方案 A:企微「会话内容存档」官方 API

- 采购官方存档许可(服务版 900 元/人/年,5 人起购,服务商 75 折约 675/人/年)
- 用官方 SDK 增量拉密文 → 本地 RSA 解密 → 入库
- **数据完整、合规、可扩展**,但采购 + 法务 + 开发周期约 1 个月

### 方案 B:RPA 自动化企微客户端

- 定时脚本驱动本人企微号,逐群读今天消息,提取文本
- 送给自己接的 LLM(Claude/GPT API)按规则判定
- **0 采购,1 周上线**;但存在账号封禁风险、单点故障、脆弱

### 方案 C:RPA + 大圆 AI 混合

- 定时脚本逐群唤起大圆,喂固定 prompt("按规则 X/Y/Z 判定")
- 抓取大圆输出 → 解析入库
- 前提:**大圆已灰度到贵司账号**(需自测)
- **0 采购,3-5 天开发**;风险同方案 B,叠加大圆稳定性风险

---

## 四、推荐路径:分阶段落地

### 阶段 1(2-4 周)- MVP 验证

**目标**:低成本跑通全链路,验证规则命中率

- 用**专用督学主管企微号**(不是老板本人号)加入所有群
- 走**方案 C(优先)**或**方案 B(大圆没灰度到时)**
- RPA 每天 22:00 跑一轮,每群间隔 3-5 秒(拟人化,总耗时 30-40 分钟)
- 输出写本地 SQLite(结果) + JSON 每日快照

**DoD**:
- 至少覆盖 100 个群试点跑 1 周
- 判定准确率 ≥ 90%(人工抽检 30 个群对比)
- 无账号异常/封禁事件

### 阶段 2(第 5-8 周)- 稳定性观察 + 采购启动

- 观察 RPA 稳定性(是否有 UI 变更、大圆抽风、账号提醒)
- 同步启动方案 A 采购:找服务商询价、走法务合同条款
- 若 RPA 频繁翻车 → 直接切方案 A,不等 8 周

**DoD**:
- 方案 A 采购走完付款 + 存档许可开通
- RPA 阶段跑够 4 周,故障率 < 5%

### 阶段 3(第 9-12 周)- 方案 A 上线,RPA 下线

- 存档 API 拉取器 + 规则引擎上线
- 数据入本地 SQLite
- RPA 停用

**DoD**:
- 所有 400-500 群覆盖
- 数据链路 end-to-end 跑通,7 天无中断
- 主管每天早上能拿到前一天判定结果

---

## 五、技术栈

### MVP 阶段(方案 B/C) —— 已推翻,见下

原本准备 RPA 抓 Mac 企微客户端 UI。2026-07-27 发现企微「API 模式机器人 + 授权用户身份读消息」能力,主路径改为 MCP(见下)。原 RPA 保留为备路。

### 现方案:MCP 主路(推荐)

| 层 | 选型 |
|---|---|
| 消息读取 | **wecom-cli**(企微官方 CLI) via subprocess |
| 授权模式 | API 模式机器人 + 用户授权「消息」权限(7 天 renew) |
| 覆盖范围 | 用户所在的所有群(理论含外部群,待联调验证) |
| 存储 | students.csv + checks.csv 两张表 |
| 通知 | 企微群机器人 webhook(推给内部管理群) |
| 调度 & 保活 | **workbuddy**(客户机托管) |
| 备路(fallback) | Mac Accessibility API RPA,仅当 MCP 不覆盖外部群时启用 |

### 正式阶段(方案 A)

| 层 | 选型 |
|---|---|
| 数据拉取 | 企微「会话内容存档」官方 SDK(Python wrapper) |
| 加解密 | RSA + AES(SDK 自带) |
| 存储 | Postgres(Neon) |
| 规则引擎 | YAML 配置 + Python 判定器(不用 LLM) |
| AI 增强(可选) | 未完成群调 Claude API 生成"改进建议" |
| 展示 | 本地 SQLite(结果) + JSON 每日快照 |
| 调度 | 云函数 / 服务器 cron |

---

## 六、数据模型(实际实现)

两张 CSV 组成本地数据库,都放 `data/` 下。

### `students.csv` —— 学生主表(从 xlsx 每日 upsert)

| 字段 | 说明 |
|---|---|
| group_id | 归一化学生编号(去掉"续/续费"前缀),如 `e001` |
| raw_id | xlsx 原始编号,如 `续e001` |
| subject | 科目原文,如 `英语, 政治` |
| tutor | 接单人;单科取"接单人"列,考研类合并"英语:X / 政治:Y" |
| start | 服务开始时间 |
| expiry | 服务到期时间 |
| source_sheet | 来自哪个 sheet |
| first_seen | 首次进入本表的日期(留档追溯用) |
| last_seen | 最近一次仍在 roster 的日期 |
| active | yes/no;xlsx 里已消失的学生标 no,不删行 |

### `checks.csv` —— 每日判定历史(append-only,RETENTION_DAYS 天前的行自动清)

| 字段 | 说明 |
|---|---|
| date | 判定日期 |
| group_id | 学生编号(关联 students.csv) |
| subject / tutor | 冗余存一份,方便主管直接看 |
| status | 完成 / 未完成 / 数据不全 |
| missing | `|` 分隔的缺失 hashtag 列表 |
| evidence | 命中的证据消息摘录 |
| checked_at | 判定运行时间 |

---

## 七、规则(实际实现)

见 [rules.yaml](rules.yaml)。核心两条:

1. 群里当日必须出现 `#今日打卡` 字样
2. 学生每一门科目 X,群里当日必须出现 `#X今日计划` 字样(考研"英语, 政治"要 `#英语今日计划` + `#政治今日计划` 两条都有)

科目字段用 `[,+、，/]+` 拆分。规则文本可直接改 YAML,无需动代码。

---

## 八、关键风险

| 风险 | 影响 | 应对 |
|---|---|---|
| RPA 账号封禁 | 业务停摆 | 用专号非老板号 / 拟人化控速 / 尽早切方案 A |
| 大圆没灰度到贵司 | 方案 C 走不通 | 退回方案 B(自接 LLM)或直接上方案 A |
| 客户拒绝存档(方案 A) | 数据不全 | **不勾选强制存档**,拒绝的群标"数据不全"给主管人工复核 |
| 家长质疑隐私 | 公关/退费风险 | 合同加告知条款 + 建群开场白二次告知 |
| 大圆输出格式不稳定 | 解析失败 | Prompt 里强制结构化 JSON 输出 + 失败重试 |
| 客户机断电/断网/企微登出 | 任务当天不跑 | workbuddy 健康检查 + 心跳(渠道 TBD) |
| 客户机环境跟本机不一致 | RPA 脚本本机跑通、客户机崩 | 早期就在客户机上做联调,不要憋到最后才部署 |

---

## 九、待决定事项(阻塞项)

按优先级:

1. **RPA 用哪个企微号**?老板号 / 专用主管号 / 本人号 → 决定风险等级
2. **能否开一个"督学主管"专号**?能 → 阶段 1 立刻启动
3. **大圆是否已灰度到贵司**?打开企微看侧边栏 → 决定走方案 B 还是 C
4. **老板 / 法务是否支持方案 A 采购**?→ 决定阶段 2 能否启动
5. **规则清单最终版**?→ 直接决定 prompt / YAML 内容

---

## 十、下一步

1. 本周内验证第 9 节的 1、2、3 项
2. 阶段 1 MVP 开发从"选定 RPA 工具 + 写第一个群的抓取脚本"开始
3. 同步准备《客户会话存档告知同意书》草稿(法务)

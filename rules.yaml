# 每日判定规则
#
# 每天扫两项:
#   1) 群里当日是否出现 "打卡标签"
#   2) 学生每一门科目是否都出现 "该科计划标签"
#
# 修改 tag 直接改这里,不用改代码。

checkin_tag: "#今日打卡"

# {subject} 会被替换成学生的每一门科目
plan_tag_template: "#{subject}今日计划"

# 科目字段拆分正则 ("英语, 政治" / "行测+申论" 都要拆成单项)
subject_split_regex: "[,+、，/]+"
